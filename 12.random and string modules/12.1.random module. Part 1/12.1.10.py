# Функция seed() используется для инициализации начального значения генератора псевдослучайных чисел в модуле random. 
# Верно ли, что по умолчанию, начальным значением генератора является системное время (текущая дата и время)?
# The seed() function is used to initialize the seed value of the pseudo-random number generator in the random module. 
# Is it true that by default, the initial value of the generator is the system time (current date and time)?

да
